name: GitHub Actions
on:
  push:
    branches:
      - $default-branch
  push:
jobs:
  I-Change-This-To-Anything-I-Want:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
